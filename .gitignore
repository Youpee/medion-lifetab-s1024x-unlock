# build workspace
.work/
.work-magisk/
.work-build.log
*.log
medion-fixup.zip

# never commit firmware / backups / built images (proprietary + huge)
*.img
*.bin
backup_nv/
vbmeta_disable.img
super_unkiosk.img

# APKs / downloaded app suite are redistributed from their own sources, never committed
*.apk
apps/
